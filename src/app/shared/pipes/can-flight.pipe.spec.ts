import { CanFlightPipe } from './can-flight.pipe';

describe('CanFlightPipe', () => {
  it('create an instance', () => {
    const pipe = new CanFlightPipe();
    expect(pipe).toBeTruthy();
  });
});
